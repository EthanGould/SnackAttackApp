<ul>
  <% @foods_array.each do |food| %>
    <li><%= link_to food.name, food_path(food)%><%= button_to 'add',  food_shopping_list_food_relations_path(food) , class: "btn btn-success", role: "button" %></li>
  <% end %>
</ul>
